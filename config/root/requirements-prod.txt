# Trading Bot v10 Enterprise - Dependencias de Producción
# ======================================================
# Dependencias optimizadas para producción

# ==================== CORE PRODUCTION ====================
# Core dependencies (optimized versions)
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
scipy>=1.11.0,<2.0.0

# Deep Learning (production optimized)
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
torchaudio>=2.0.0,<3.0.0
pytorch-lightning>=2.0.0,<3.0.0

# MLflow para experiment tracking
mlflow>=2.5.0,<3.0.0
optuna>=3.2.0,<4.0.0

# ==================== TRADING & EXCHANGE ====================
# Exchange APIs (production)
ccxt[async]>=4.0.0,<5.0.0

# WebSocket y conexiones (production)
websockets>=11.0.0,<12.0.0
aiohttp>=3.8.0,<4.0.0
aiofiles>=23.0.0,<24.0.0

# ==================== DATABASE & STORAGE ====================
# PostgreSQL y TimescaleDB (production)
psycopg2-binary>=2.9.0,<3.0.0
asyncpg>=0.28.0,<1.0.0

# Redis para caching (production)
redis>=4.6.0,<5.0.0
aioredis>=2.0.0,<3.0.0

# Parquet (production)
pyarrow>=12.0.0,<13.0.0

# ==================== MESSAGE QUEUING ====================
# Kafka (production)
kafka-python>=2.0.0,<3.0.0
aiokafka>=0.8.0,<1.0.0

# ==================== MONITORING & METRICS ====================
# Prometheus (production)
prometheus-client>=0.17.0,<1.0.0

# Logging (production)
structlog>=23.1.0,<24.0.0
colorlog>=6.7.0,<7.0.0

# ==================== TECHNICAL ANALYSIS ====================
# Indicadores técnicos (production)
TA-Lib>=0.6.6,<1.0.0
talib-binary>=0.6.6,<1.0.0
pandas-ta>=0.3.14b0

# ==================== ASYNC & CONCURRENCY ====================
# Async support (production)
asyncio-mqtt>=0.13.0,<1.0.0
asyncio-throttle>=1.0.2,<2.0.0

# Retry y resilencia (production)
tenacity>=8.2.0,<9.0.0
backoff>=2.2.0,<3.0.0

# ==================== CONFIGURATION & SECURITY ====================
# Configuración (production)
PyYAML>=6.0,<7.0.0
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0,<3.0.0

# Seguridad (production)
cryptography>=41.0.0,<42.0.0
pyjwt>=2.8.0,<3.0.0
bcrypt>=4.0.0,<5.0.0

# ==================== DATA PROCESSING ====================
# Procesamiento (production)
dask>=2023.5.0,<2024.0.0
numba>=0.57.0,<1.0.0

# Serialización (production)
msgpack>=1.0.0,<2.0.0
orjson>=3.9.0,<4.0.0

# ==================== WEB & API ====================
# FastAPI (production)
fastapi>=0.100.0,<1.0.0
uvicorn[standard]>=0.22.0,<1.0.0
gunicorn>=21.2.0,<22.0.0

# ==================== DASHBOARD ====================
# Dashboards (production)
dash>=2.14.0,<3.0.0
dash-bootstrap-components>=1.4.0,<2.0.0
plotly>=5.15.0,<6.0.0

# ==================== UTILITIES ====================
# Utilidades (production)
tqdm>=4.65.0,<5.0.0
click>=8.1.0,<9.0.0
rich>=13.4.0,<14.0.0

# Fechas (production)
python-dateutil>=2.8.0,<3.0.0
pytz>=2023.3

# HTTP (production)
requests>=2.31.0,<3.0.0
httpx>=0.24.0,<1.0.0

# ==================== CLOUD & DEPLOYMENT ====================
# Docker (production)
docker>=6.1.0,<7.0.0

# AWS (production)
boto3>=1.28.0,<2.0.0
botocore>=1.31.0,<2.0.0

# ==================== MONITORING PROD ====================
# Sentry para error tracking (production)
sentry-sdk>=1.28.0,<2.0.0

# ==================== INSTALLATION ====================
# Para instalar dependencias de producción:
# pip install -r requirements-prod.txt
#
# Para instalar con constraints de seguridad:
# pip install -r requirements-prod.txt --constraint constraints.txt
