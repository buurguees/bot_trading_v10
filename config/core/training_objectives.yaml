# =================================================================
# OBJETIVOS DE ENTRENAMIENTO - Bot Trading v10 Enterprise
# =================================================================
# Objetivos REALISTAS y SOSTENIBLES para trading profesional
# Enfocados en gestión de riesgo y crecimiento consistente

# ⚙️ CONFIGURACIÓN DE MODOS DE ENTRENAMIENTO HISTÓRICO
historical_training_modes:
  ultra_fast:
    name: "Ultra Rápido"
    description: "Entrenamiento rápido para pruebas y desarrollo"
    days: 30
    cycles: 50
    chunk_size_days: 7
    chunk_overlap_days: 1
    max_memory_mb: 2048
    progress_report_interval: 10
    use_case: "Desarrollo, pruebas rápidas, validación de estrategias"
    
  fast:
    name: "Rápido"
    description: "Entrenamiento acelerado con datos recientes"
    days: 90
    cycles: 100
    chunk_size_days: 14
    chunk_overlap_days: 2
    max_memory_mb: 4096
    progress_report_interval: 5
    use_case: "Análisis de tendencias recientes, optimización rápida"
    
  normal:
    name: "Normal"
    description: "Entrenamiento estándar con datos históricos completos"
    days: 180
    cycles: 200
    chunk_size_days: 30
    chunk_overlap_days: 3
    max_memory_mb: 8192
    progress_report_interval: 2
    use_case: "Entrenamiento estándar, análisis de mercado completo"
    
  complete:
    name: "Completo"
    description: "Entrenamiento exhaustivo con máximo de datos históricos"
    days: 365
    cycles: 500
    chunk_size_days: 60
    chunk_overlap_days: 5
    max_memory_mb: 16384
    progress_report_interval: 1
    use_case: "Análisis exhaustivo, entrenamiento de producción"
    
  realistic:
    name: "Realista"
    description: "Entrenamiento 100% realista con datos históricos cronológicos"
    days: 365
    cycles: 500
    chunk_size_days: 60
    chunk_overlap_days: 5
    max_memory_mb: 16384
    progress_report_interval: 1
    use_case: "Entrenamiento realista, simulación de trading real"
    realistic_mode: true
    max_daily_roi: 2.0  # Máximo 2% ROI diario
    max_annual_roi: 50.0  # Máximo 50% ROI anual
    commission_rate: 0.001  # 0.1% comisión por trade
    spread_rate: 0.0005  # 0.05% spread
    slippage_rate: 0.0002  # 0.02% slippage
    max_leverage: 5.0  # Máximo 5x leverage
    realistic_volatility: true

# 🔧 CONFIGURACIÓN POR DEFECTO DE ENTRENAMIENTO
default_training_config:
  mode: "realistic"
  incremental_training: true
  enable_cycle_telegram: true
  enable_cycle_metrics: true
  save_strategies: true
  save_session_data: true

# 💰 OBJETIVOS FINANCIEROS REALISTAS
financial_targets:
  balance:
    initial: 1000.0              # Balance inicial para training
    target: 5000.0               # Balance objetivo realista (5x)
    intermediate_milestones:     # Metas progresivas
      - 1250.0    # 1.25x (25% gain)
      - 1500.0    # 1.5x (50% gain)
      - 2000.0    # 2x (100% gain)
      - 3000.0    # 3x (200% gain)
      - 4000.0    # 4x (300% gain)
      
  roi:
    target_pct: 400.0            # ROI objetivo realista (5x = 400%)
    monthly_target_pct: 12.0     # ROI mensual sostenible
    weekly_target_pct: 2.5       # ROI semanal realista
    daily_target_pct: 0.3        # ROI diario conservador
    
  r_ratio:
    target: 2.5                  # Risk/Reward ratio objetivo
    minimum_acceptable: 1.8      # R mínimo aceptable
    excellent: 3.5               # R excelente
    
  risk_metrics:
    max_drawdown_pct: 12.0       # Máximo drawdown aceptable
    max_consecutive_losses: 3    # Máximas pérdidas consecutivas
    var_95_pct: 3.0              # Value at Risk al 95%
    max_risk_per_trade_pct: 2.0  # Máximo riesgo por trade

# 📊 OBJETIVOS DE PERFORMANCE REALISTAS
performance_targets:
  win_rate:
    target_pct: 65.0             # Win rate objetivo alcanzable
    minimum_acceptable_pct: 55.0 # Win rate mínimo viable
    excellent_pct: 75.0          # Win rate excelente
    
  sharpe_ratio:
    target: 2.0                  # Sharpe ratio objetivo
    minimum_acceptable: 1.2      # Sharpe mínimo aceptable
    excellent: 3.0               # Sharpe excelente
    
  calmar_ratio:
    target: 1.8                  # Calmar ratio objetivo
    minimum_acceptable: 1.0      # Calmar mínimo aceptable
    
  profit_factor:
    target: 1.8                  # Profit factor objetivo
    minimum_acceptable: 1.3      # Profit factor mínimo
    excellent: 2.5               # Profit factor excelente

# 🤖 OBJETIVOS DE IA/ML
ml_targets:
  model_accuracy:
    target_pct: 70.0             # Precisión objetivo realista
    minimum_acceptable_pct: 60.0 # Precisión mínima aceptable
    excellent_pct: 80.0          # Precisión excelente
    
  prediction_confidence:
    target_pct: 75.0             # Confianza objetivo
    minimum_trading_pct: 60.0    # Confianza mínima para tradear
    high_confidence_threshold: 85.0  # Umbral alta confianza
    
  model_stability:
    max_deviation_pct: 8.0       # Máxima desviación en predicciones
    consistency_window_days: 7   # Ventana para medir consistencia

# ⚡ OBJETIVOS OPERACIONALES
operational_targets:
  execution:
    max_latency_ms: 150          # Latencia máxima aceptable
    target_latency_ms: 80        # Latencia objetivo
    uptime_pct: 99.0             # Uptime objetivo
    
  trades:
    daily_target_range: [5, 25]  # Rango objetivo de trades diarios
    monthly_target_range: [150, 600]  # Rango mensual
    max_daily_trades: 50         # Límite para prevenir overtrading
    
  efficiency:
    cpu_usage_max_pct: 75.0      # Uso máximo de CPU
    memory_usage_max_pct: 70.0   # Uso máximo de memoria

# 🎯 OBJETIVOS POR TIMEFRAME
timeframe_targets:
  scalping:    # 1m, 5m
    win_rate_pct: 62.0
    r_ratio: 1.8
    trades_per_day_range: [15, 35]
    max_leverage: 15
    
  intraday:    # 15m, 1h
    win_rate_pct: 68.0
    r_ratio: 2.2
    trades_per_day_range: [8, 20]
    max_leverage: 12
    
  swing:       # 4h, 1d
    win_rate_pct: 72.0
    r_ratio: 2.8
    trades_per_week_range: [5, 15]
    max_leverage: 8

# 📈 OBJETIVOS POR SÍMBOLO (CONSERVADORES)
symbol_targets:
  BTCUSDT:
    monthly_roi_pct: 15.0        # ROI mensual conservador
    win_rate_pct: 65.0
    min_volume: 1000000
    max_leverage: 20
    risk_per_trade_pct: 1.5
    
  ETHUSDT:
    monthly_roi_pct: 18.0
    win_rate_pct: 62.0
    min_volume: 500000
    max_leverage: 15
    risk_per_trade_pct: 2.0
    
  ADAUSDT:
    monthly_roi_pct: 22.0
    win_rate_pct: 58.0
    min_volume: 100000
    max_leverage: 12
    risk_per_trade_pct: 2.5

# 🏆 SISTEMA DE LOGROS PROGRESIVOS
achievements:
  milestones:
    first_profit_day: "Primer día rentable consecutivo"
    first_week_profit: "Primera semana rentable"
    first_month_profit: "Primer mes rentable"
    risk_management_master: "30 días sin exceder límites de riesgo"
    consistency_expert: "Win rate >60% durante 2 semanas"
    
  performance_tiers:
    bronze:
      win_rate_min_pct: 55.0
      roi_min_pct: 25.0
      max_drawdown_pct: 15.0
      
    silver:
      win_rate_min_pct: 62.0
      roi_min_pct: 75.0
      max_drawdown_pct: 12.0
      
    gold:
      win_rate_min_pct: 68.0
      roi_min_pct: 150.0
      max_drawdown_pct: 10.0
      
    platinum:
      win_rate_min_pct: 72.0
      roi_min_pct: 300.0
      max_drawdown_pct: 8.0

# 🚨 LÍMITES DE SEGURIDAD ESTRICTOS
safety_limits:
  emergency_stop:
    max_daily_loss_pct: 4.0      # Stop si pérdida diaria > 4%
    max_drawdown_pct: 12.0       # Stop si drawdown > 12%
    consecutive_losses: 3        # Stop si 3 pérdidas consecutivas
    max_leverage_global: 25      # Leverage máximo absoluto
    
  warning_thresholds:
    daily_loss_warning_pct: 2.5  # Warning si pérdida > 2.5%
    drawdown_warning_pct: 8.0    # Warning si drawdown > 8%
    low_confidence_warning_pct: 50.0  # Warning si confianza < 50%
    overtrading_warning: 40      # Warning si > 40 trades/día

# 📊 MÉTRICAS DE EVALUACIÓN BALANCEADAS
evaluation_metrics:
  primary:
    - "total_roi_pct"
    - "win_rate_pct" 
    - "sharpe_ratio"
    - "max_drawdown_pct"
    - "risk_adjusted_return"
    
  secondary:
    - "profit_factor"
    - "calmar_ratio"
    - "model_accuracy_pct"
    - "avg_trade_duration_hours"
    - "consistency_score"
    
  weights:  # Pesos balanceados para scoring
    roi: 0.25
    win_rate: 0.20
    sharpe: 0.20
    drawdown: 0.20
    consistency: 0.15

# 🔄 CONFIGURACIÓN DE RESETS CONSERVADORES
reset_conditions:
  auto_reset:
    enabled: true
    triggers:
      - "drawdown > 12.0"
      - "consecutive_losses >= 3"
      - "daily_loss > 4.0"
      - "leverage_violation_count >= 3"
      
  reset_strategy:
    balance_reset: true
    model_penalty: true
    strategy_cooldown: true      # Cooldown de estrategias problemáticas
    leverage_reduction: true     # Reducir leverage tras reset

# ⚙️ CONFIGURACIÓN DE MODOS DE ENTRENAMIENTO
training_modes:
  conservative:
    name: "Conservador"
    description: "Enfoque en preservación de capital"
    max_risk_per_trade: 1.0
    max_leverage: 10
    target_win_rate: 70.0
    
  balanced:
    name: "Balanceado"
    description: "Balance entre riesgo y rendimiento"
    max_risk_per_trade: 1.5
    max_leverage: 15
    target_win_rate: 65.0
    
  aggressive:
    name: "Agresivo"
    description: "Mayor riesgo para mayor rendimiento"
    max_risk_per_trade: 2.0
    max_leverage: 20
    target_win_rate: 60.0

# 🎯 ANÁLISIS JERÁRQUICO MEJORADO
hierarchical_analysis:
  enabled: true
  
  analysis_levels:
    strategic:
      timeframes: ["1d", "4h"]
      purpose: "Detectar tendencias principales"
      indicators: ["rsi", "sma_20", "sma_50", "macd"]
      weight: 0.4
      confidence_threshold: 70.0
      
    tactical:
      timeframes: ["1h", "15m"]
      purpose: "Confirmar direcciones y timing"
      indicators: ["rsi", "sma_20", "macd", "bb_upper", "bb_lower"]
      weight: 0.35
      confidence_threshold: 65.0
      
    execution:
      timeframes: ["5m", "1m"]
      purpose: "Ejecutar trades con precisión"
      indicators: ["rsi", "volatility", "volume_sma"]
      weight: 0.25
      confidence_threshold: 60.0
  
  decision_logic:
    require_strategic_alignment: true
    min_tactical_confirmation: 0.6
    min_execution_confidence: 0.6
    
    risk_filters:
      min_volatility: 0.005
      max_volatility: 0.08
      min_volume_ratio: 1.2

# 🎮 SISTEMA DE GAMIFICACIÓN SANA
gamification:
  daily_challenges:
    risk_management: "No exceder 2% de riesgo por trade"
    consistency: "Mantener win rate >60%"
    discipline: "No más de 25 trades"
    
  weekly_goals:
    profit_target: "2-5% de ganancia semanal"
    drawdown_control: "Máximo 6% de drawdown"
    accuracy_improvement: "Mejorar precisión del modelo"

# 📚 SISTEMA DE APRENDIZAJE ADAPTATIVO
adaptive_learning:
  performance_feedback:
    good_trade_threshold: 1.5    # R ratio mínimo para "buen trade"
    excellent_trade_threshold: 2.5
    
  strategy_evolution:
    min_sample_size: 20          # Mínimo trades para evaluar estrategia
    confidence_decay: 0.95       # Decay de confianza en estrategias
    
  risk_adaptation:
    increase_risk_threshold: 0.75  # Win rate para aumentar riesgo
    decrease_risk_threshold: 0.55  # Win rate para disminuir riesgo

# 🔧 CONFIGURACIÓN POR DEFECTO
default_training_mode: "balanced"
default_risk_level: "moderate"
enable_paper_trading: true
enable_risk_monitoring: true